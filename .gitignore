# Secrets
.env

__pycache__/

# User data: now in %APPDATA%\AccidentAI (these only exist in older checkouts, before migration)
settings.json
uploads_pending.json
Recordings/
Accidents_Screen/

# Large model files (see README for where to get them) and local model backups
assets/yolo/yolov3.weights
assets/model/model_weights.h5
assets/model/accident_model*.keras
assets/model/backup_*/

# Downloaded training images (training/download_training_data.py) and test videos
data/**/hf_cctv_*
data/**/detrac_*
samples/videos/accident_*.mp4

# Build output (packaging/)
build/
dist/
